{
  "name": "eclypse-coming-soon-page-v1",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "concurrently": "^9.1.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "concurrently \"bun --cwd backend run start\" \"bun --cwd frontend run dev\"",
    "vercel-build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "glob": "^11.0.2",
    "sonner": "^2.0.5"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
